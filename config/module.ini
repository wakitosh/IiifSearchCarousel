[info]
name = "IIIF Search Carousel"
version = "0.2.35"
author       = "Toshihito Waki"
author_link  = "https://github.com/wakitosh"
module_link  = "https://github.com/wakitosh/iiif_search_carousel"
description = "Search box block with background image carousel (IIIF-based). Supports strict AND/OR, logic persistence, and MeCab-aware example keywords (no fallback, capped to 4). Multilingual stopwords applied; min token length aligns with backend (>=4 default, >=2 on Mroonga)."
config_version = 1
omeka_version_constraint = "^4.0.0"
license = "GPL-3.0-or-later"
support_link = "https://github.com/wakitosh/iiif_search_carousel"
configurable = true
